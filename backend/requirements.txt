# FastAPI and Server
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.9

# Supabase
supabase==2.27.1

# Password Hashing
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.1.0.post1

# File handling
python-magic==0.4.27
Pillow==10.2.0
aiofiles==23.2.1

# Redis (for rate limiting and caching)
redis==5.0.1

# JWT Decoding
PyJWT==2.10.1

# Utilities
python-dotenv==1.0.1

# HTTP Client
httpx==0.28.1

# AI 发帖助手 (V3 #25): OpenAI 兼容协议,统一调用 DeepSeek / Qwen / Moonshot.
# Qwen-VL 视觉模型也走这个 SDK,无需额外多模态库。
openai==1.59.5

# AI 发帖助手图片内容安全: 阿里云内容审核 ImageAudit (Green-CIP) 同步接口
# alibabacloud_tea_openapi 由 imageaudit 自带传递依赖 (>=0.3.16,<1.0.0),
# 不要显式 pin,否则会跟 SDK 自己的版本约束冲突。
alibabacloud_imageaudit20191230==2.0.6

# 订单/钱包定时任务调度。lifespan 内启动 AsyncIOScheduler,
# 替代「POST /api/admin/orders/scheduler/run」手动触发。
# 通过 settings.ENABLE_BACKGROUND_SCHEDULER 关闭,便于本地调试或单测。
apscheduler==3.11.0

# 实名认证 / 身份证号字段 AES 加密 (Fernet)。
# KYC_ENCRYPTION_KEY 未配置时服务层会拒绝写库,避免明文落盘。
cryptography==44.0.0
